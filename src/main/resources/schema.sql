CREATE TABLE IF NOT EXISTS tracks (
                                      dugf BIGINT,
                                      hhrs BIGINT,
                                      lcount BIGINT,
                                      btrfsnum BIGINT,
                                      pod_id BIGINT,
                                      pod_tssnum TIMESTAMP WITHOUT TIME ZONE
);